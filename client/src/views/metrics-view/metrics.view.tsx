import React, { ReactElement } from 'react';

import MainLayout from 'layouts/main-layout/main.layout';

const MetricsView = (): ReactElement => {
  return <MainLayout>Metrics view.</MainLayout>;
};

export default MetricsView;
