import React, { ReactElement } from 'react';

import Settings from 'components/Settings';

const SettingsView = (): ReactElement => <Settings dataTest="SettingsView" />;

export default SettingsView;
