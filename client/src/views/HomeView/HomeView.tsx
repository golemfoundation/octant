import React, { ReactElement } from 'react';

import Layout from 'components/shared/Layout';

const HomeView = (): ReactElement => {
  return <Layout dataTest="HomeView">HOME</Layout>;
};

export default HomeView;
