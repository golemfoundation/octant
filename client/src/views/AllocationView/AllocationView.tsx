import React, { ReactElement } from 'react';

import Allocation from 'components/Allocation';

const AllocationView = (): ReactElement => <Allocation dataTest="AllocationView" />;

export default AllocationView;
